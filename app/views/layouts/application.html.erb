<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Billing System" %></title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
  </head>
  <body>

    <div class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <a class="navbar-brand" href="<%= root_path %>">Home</a>
        <a class="navbar-brand" href="<%= users_path %>">Users</a>
        <a class="navbar-brand" href="<%= event_types_path %>">Event Type</a>
        <a class="navbar-brand" href="<%= events_path %>">Events</a>
        <a class="navbar-brand" href="<%= monthly_report_path %>">Monthly Report</a>
      </div>
    </div>
    <hr>
    <div class="container">
      <div class="row">
        <div class="col-lg-9">
          <%= bootstrap_flash %>
          <%= yield %>
        </div>
      </div><!--/row-->
    </div> <!-- /container -->

  </body>
</html>
